/*
    class gameService creates gameState map & gameLoop map with NodeJS.Timer
    creategame function waits for gamemodel to register both players in database -> returns the game further up
    startGameLoop function for setting readiness interval and starting up game.
    updateGameState & handlePlayermove functions each frame for 60 fps
*/